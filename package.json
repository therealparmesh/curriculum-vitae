{
  "name": "curriculum-vitae",
  "version": "1.0.0",
  "private": true,
  "license": "MIT",
  "scripts": {
    "build": "preact build",
    "dev": "preact watch",
    "pretty-quick": "pretty-quick",
    "serve": "preact build && serve build --single",
    "start": "per-env",
    "start:development": "npm run --silent dev",
    "start:production": "npm run --silent serve",
    "tsc": "tsc"
  },
  "dependencies": {
    "bulma": "^0.8.2",
    "bulmaswatch": "^0.8.1",
    "preact": "^10.4.4",
    "preact-render-to-string": "^5.1.8",
    "preact-router": "^3.2.1"
  },
  "devDependencies": {
    "husky": "^4.2.5",
    "node-sass": "^4.14.1",
    "per-env": "^1.0.0",
    "preact-cli": "^3.0.0-rc.13",
    "prettier": "^2.0.5",
    "pretty-quick": "^2.0.0",
    "sass-loader": "^8.0.2",
    "serve": "^11.3.1",
    "typescript": "^3.9.3"
  }
}
